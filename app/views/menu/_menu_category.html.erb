<%= link_to "All", menu_index_path(category:"all"),method: :get%><br>
<% MenuCategory.order("name").all.each do |category|%>
  <div>
     <%= link_to category.name , menu_index_path(category:category.id),method: :get %>
  </div>
<% end %>
